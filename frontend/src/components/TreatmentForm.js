
/*import { Button, Col, Form, Input, Row } from "antd";
import moment from "moment";
import React from "react";

function TreatmentForm({ onFinish, initivalValues }) {
  return (
    <Form
      layout="vertical"
      onFinish={onFinish}
      initialValues={{
        ...initivalValues,
        ...(initivalValues 
          
        ),
      }}
    >
      <h1 className="card-title mt-3">Personal Information</h1>
      <Row gutter={20}>
        <Col span={8} xs={24} sm={24} lg={8}>
          <Form.Item
            required
            label="Therapy"
            name="therapy"
            rules={[{ required: true }]}
          >
            <Input placeholder="Therapy" />
          </Form.Item>
        </Col>
        <Col span={8} xs={24} sm={24} lg={8}>
          <Form.Item
            required
            label="Price"
            name="price"
            rules={[{ required: true }]}
          >
            <Input placeholder="price" />
          </Form.Item>
        </Col>
        
        
      </Row>
      <hr />
     
      <div className="d-flex justify-content-end">
        <Button className="primary-button" htmlType="submit">
          SUBMIT
        </Button>
      </div>
    </Form>
  );
}

export default TreatmentForm; */
